{{#if showWasPrice}}
    <div class="was-price-container">
        <span class="was-price-price" data-rate="{{wasPrice}}">{{wasPriceFormatted}}</span>
        <p class="now">{{translate 'NOW'}}</p>
    </div>
{{/if }}
